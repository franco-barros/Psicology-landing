export interface ArticleItem {
  id: string;
  title: string;
  content: string;
  image?: string;
}

export interface TestimonialItem {
  id: string;
  name: string;
  message: string;
  image: string;
}

export interface TipItem {
  id: string;
  title: string;
  text: string;
  tag: string;
}
